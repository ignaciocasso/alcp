#rabin algorithm

irreducible := proc(g,n,p,q,x)  #comprueba si el polinomio g sobre x de grado n con coeficientes en q=p'k es irreducible
  a := Powmod(x,q^n,f,x) mod p;
  if a<>x then
    return false;
  end if;
  for t from 2 to iquo(n,2) do
    if isprime(t) and irem(n,t)==0 then
      a:=Powmod(x,q^(n/t),f,x) mod p;
      if Gcd(b-x,f) mod p <> 1 then #funcionara para Fq?
        return false;
      end if;
    end if;
  end do;
  return true;
end proc;
